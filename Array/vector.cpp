#include <iostream>
#include <vector>
#include <algorithm>
using namespace std;
int main(){
    vector<int> arr;
    int size = (sizeof(arr)/sizeof(int));
    cout << size;
    cout << endl;
    
    cout << sizeof(arr);

}